{
  "name": "faculty-management-backend",
  "version": "1.0.0",
  "description": "Backend for Faculty Student Management System",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc && cd ../frontend && npm run build",
    "build-backend": "tsc",
    "start": "node dist/server.js",
    "seed-admin": "npm run build-backend && node dist/seed-admin.js",
    "dev": "ts-node src/server.ts",
    "watch": "nodemon --exec ts-node src/server.ts",
    "testBackend": "NODE_ENV=test tsx --test",
    "migrate": "ts-node src/migrate.ts",
    "test": "node --require ts-node/register --test --test-concurrency=1 test/*.test.ts"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/pg": "^8.10.2",
    "@types/cors": "^2.8.13",
    "@types/node": "^20.4.5",
    "typescript": "^5.1.6",
    "ts-node": "^10.9.1",
    "tsx": "^4.22.3",
    "nodemon": "^3.0.1"
  }
}